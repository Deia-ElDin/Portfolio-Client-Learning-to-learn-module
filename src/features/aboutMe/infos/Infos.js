import LeftInfos from './LeftInfos';
import RightInfos from './RightInfos';

const Infos = () => {
  return (
    <section className="infos" aria-label="Infos">
      <LeftInfos />
      <RightInfos />
    </section>
  );
};

export default Infos;
